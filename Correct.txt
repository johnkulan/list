1. СДЕЛАНО. Задача.
  
 Я добавляю в список следующих студентов:
	1. Mike		ggg	4
	2. John		sm	4
	3. Rose		hhh	3.9
Теперь я хочу с помощью команды 4.find найти их индексы
	1. Гуглю Mike:
	The number of your member is 1
	2. Гуглю John:
	The number of your member is 2
	3. Гуглю Rose:
	The list doesn't contain this shit

Вопрос: почему программа не может найти Rose в списке?

код поиска студента исправлен. урок понял: доверяй но проверяй.

template <typename T_>
int LIST<T_>::indexOf(T_ * elem, bool (*equal)(T_*, T_*))
{
	int temp = -1;
	for (int i = 0; i < count; i++)
		if (equal(arr + i, elem))
			temp = i;
	return temp;
}

2. СДЕЛАНО. Работа с файлами.